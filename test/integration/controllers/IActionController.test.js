'use strict'
/* global describe, it */

const assert = require('assert')

describe('IActionController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['IActionController'])
  })
})
