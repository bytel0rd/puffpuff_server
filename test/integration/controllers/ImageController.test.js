'use strict'
/* global describe, it */

const assert = require('assert')

describe('ImageController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ImageController'])
  })
})
