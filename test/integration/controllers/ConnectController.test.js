'use strict'
/* global describe, it */

const assert = require('assert')

describe('ConnectController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ConnectController'])
  })
})
