'use strict'
/* global describe, it */

const assert = require('assert')

describe('FlourController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['FlourController'])
  })
})
