'use strict'
/* global describe, it */

const assert = require('assert')

describe('CommentController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['CommentController'])
  })
})
