'use strict'
/* global describe, it */

const assert = require('assert')

describe('BootIActionService', () => {
  it('should exist', () => {
    assert(global.app.api.services['BootIActionService'])
  })
})
