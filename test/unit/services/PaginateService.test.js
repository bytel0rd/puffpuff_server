'use strict'
/* global describe, it */

const assert = require('assert')

describe('PaginateService', () => {
  it('should exist', () => {
    assert(global.app.api.services['PaginateService'])
  })
})
